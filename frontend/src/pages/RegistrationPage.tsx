import React from 'react';
import RegistrationForm from '../components/credentials/RegistrationForm';


const RegisterPage = () => {
    return (
        <div className={"container"} style={{ padding: 0 }}>
            <RegistrationForm />
        </div>
    );
};

export default RegisterPage;